% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_data.R
\name{load_data}
\alias{load_data}
\title{Load and Preprocess Methylation Data}
\usage{
load_data(
  dir_path,
  sample_sheet,
  type = NULL,
  groups = NULL,
  cores = 1,
  use_cpp = TRUE,
  single_file = NULL,
  min_coverage = 0L
)
}
\arguments{
\item{dir_path}{Path to the directory containing the data files.}

\item{sample_sheet}{Path to the sample sheet file or a data.frame/data.table containing
sample information with columns: group_id, replicate, sample_id, file_name.}

\item{type}{Character string specifying which type of files to load.
If NULL (default), it will use file names from the sample sheet as is.}

\item{groups}{Character vector of group_ids to include. If NULL (default),
all groups will be loaded.}

\item{cores}{Number of cores to use for parallel processing. Default is 1
(no parallel processing).}

\item{use_cpp}{Logical, whether to use the C++ implementation for faster loading.
Default is TRUE. Will fall back to R implementation if C++ is not available.}

\item{single_file}{Optional path to a single methylation file to load directly
When provided, the sample_sheet parameter is ignored.}

\item{min_coverage}{Integer Minimum coverage threshold. Sites with coverage
below this value are excluded during loading. Default is 0 (no filtering).}
}
\value{
A list of data.tables containing processed methylation data, or a
single data.table if single_file is provided.
}
\description{
Load methylation data from files based on a sample sheet.
}
